@using Microsoft.AspNetCore.Http.Extensions;

<style>
  .active > a {
    background-color: #F9F9F9 !important;
  }
</style>
<br />
<ul class="nav nav-tabs nav-justified">
  <li @(Context.Request.GetEncodedUrl().ToString().Contains("Administrare") ? "class = active" : "")><a href="@Url.Action("Index", "Administrare")"><span class="glyphicon glyphicon-align-justify"></span> Anunțurile mele</a></li>
  <li @(Context.Request.GetEncodedUrl().ToString().Contains("Mesaje") ? "class = active" : "")><a href="@Url.Action("Index", "Mesaje")"><span class="glyphicon glyphicon-envelope"></span> Mesaje</a></li>
  <li @(Context.Request.GetEncodedUrl().ToString().Contains("Edit") ? "class = active" : "")><a href="@Url.Action("EditUserDetails", "Manage")"><span class="glyphicon glyphicon-pencil"></span> Detalii cont</a></li>
  @*<li @(Context.Request.GetEncodedUrl().ToString().Contains("EditAgentieDetails") ? "class = active" : "")><a href="@Url.Action("EditAgentieDetails", "Manage")"><span class="glyphicon glyphicon-cog"></span> Detalii agentie</a></li>*@
  <li @(Context.Request.GetEncodedUrl().ToString().Contains("ChangePassword") ? "class = active" : "")>@Html.ActionLink("Schimbă parola", "ChangePassword", "Manage")</li>
  <li @(Context.Request.GetEncodedUrl().ToString().Contains("ManageLogins") ? "class = active" : "")>@Html.ActionLink("Login extern", "ManageLogins", "Manage")</li>
  <li @(Context.Request.GetEncodedUrl().ToString().Contains("DeleteAccount") ? "class = active" : "")><a href="@Url.Action("DeleteAccount", "Manage")"><span class="glyphicon glyphicon-remove"></span> Șterge Cont</a></li>
</ul>